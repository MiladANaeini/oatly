export type dataType = {
  title: string;
  author: string;
  category: string;
  publishedAt: string;
  prelude: string;
  paragraphs: string[];
};
export type LoadingType = {
  loading?: boolean;
  className?: string;
};
